import { useState } from "react"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";

const priorities = [ { id: "1", title: "تقویت هویت فرهنگی-ایرانی", content: [ "حمایت از تولیدات فرهنگی با محتوای بومی و ملی", "معرفی مفاخر فرهنگی، تاریخی و هنری استان", "برگزاری آیین‌ها و جشنواره‌هایی با محوریت ایران فرهنگی" ] }, { id: "2", title: "احیای هنرهای سنتی و بومی", content: [ "شناسایی، آموزش و حمایت از هنرمندان بومی", "راه‌اندازی بازارچه‌های صنایع‌دستی در شهرستان‌ها", "ثبت و مستندسازی میراث ناملموس هنری" ] }, { id: "3", title: "افزایش مشارکت فرهنگی جوانان", content: [ "خانه‌های فرهنگ محله‌ای ویژه نوجوانان", "جشنواره‌ها و رقابت‌های فرهنگی میان مدارس و دانشگاه‌ها", "تقویت تشکل‌های فرهنگی جوان‌محور در سراسر استان" ] }, { id: "4", title: "ارتقای سواد رسانه‌ای و زیست فرهنگی در فضای مجازی", content: [ "آموزش سواد رسانه‌ای برای والدین، دانش‌آموزان و معلمان", "تولید محتوای فاخر بومی در قالب‌های نوین رسانه‌ای", "شبکه‌سازی کنشگران فرهنگی در فضای مجازی استان" ] }, { id: "5", title: "ترویج فرهنگ ایثار، جهاد و شهادت", content: [ "تولید آثار نمایشی، تجسمی و مستند با محوریت جهاد", "روایت محلی دفاع مقدس در قالب برنامه‌های میدانی", "فعال‌سازی ظرفیت یادمان‌ها و موزه‌های مقاومت فرهنگی" ] } ];

export default function CulturePriorities() { const [selectedTab, setSelectedTab] = useState("1");

return ( <div className="max-w-3xl mx-auto p-6"> <h1 className="text-2xl font-bold text-center mb-6">پنج اولویت فرهنگی استان کرمانشاه</h1> <Tabs value={selectedTab} onValueChange={setSelectedTab} className="w-full"> <TabsList className="flex flex-wrap justify-center gap-2"> {priorities.map((p) => ( <TabsTrigger key={p.id} value={p.id} className="text-sm"> {p.title} </TabsTrigger> ))} </TabsList> {priorities.map((p) => ( <TabsContent key={p.id} value={p.id}> <Card className="mt-4"> <CardContent className="p-4 space-y-2 text-right"> <h2 className="text-lg font-semibold">{p.title}</h2> <ul className="list-disc pr-5 space-y-1"> {p.content.map((item, index) => ( <li key={index}>{item}</li> ))} </ul> </CardContent> </Card> </TabsContent> ))} </Tabs> </div> ); }

